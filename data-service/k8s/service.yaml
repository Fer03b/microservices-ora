apiVersion: v1
kind: Service
metadata:
  name: data-service
  labels:
    app: data-service
spec:
  type: ClusterIP
  selector:
    app: data-service
  ports:
    - name: grpc
      port: 50052
      targetPort: 50052
      protocol: TCP
